# Masgent Web Application - Streamlit Cloud Requirements
# Simplified for cloud deployment

# Core Streamlit framework
streamlit>=1.28.0

# AI Integration - Direct Google AI SDK (simpler than pydantic-ai)
google-generativeai>=0.3.0
nest-asyncio>=1.5.0

# 3D Visualization
stmol>=0.0.9
py3Dmol>=2.0.0

# Interactive charts
plotly>=5.18.0

# Materials science core (without heavy ML dependencies)
pymatgen>=2023.0.0
ase>=3.22.0
mp-api>=0.37.0

# Note: Removed pydantic-ai to avoid dm-tree build issues on Streamlit Cloud
# Using direct google-generativeai instead
